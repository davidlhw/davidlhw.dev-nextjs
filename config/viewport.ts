export type Viewport = {
  sm: string;
  md: string;
  lg: string;
  xl: string;
};

const viewport: Viewport = {
  sm: "487px",
  md: "768px",
  lg: "1024px",
  xl: "1920px",
};

export default viewport;
